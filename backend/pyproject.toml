[project]
name = "ecotrack"
version = "0.1.0"
description = "Environmental observation and data tracking platform"
authors = [
    {name = "Katlyn Stoll",email = "katlynstoll@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django (>=6.0.8,<7.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
